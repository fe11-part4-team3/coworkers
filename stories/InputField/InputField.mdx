import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
} from '@storybook/addon-docs';
import InputField from '../../components/InputField';

<Meta
  title="Components/InputField"
  component={InputField}
/>

# InputField Component

`InputField`는 사용자 입력을 처리하는 기본 컴포넌트입니다.

## Props

<ArgsTable />

## 기본 사용법

기본적으로 `value`, `label`, `placeholder`를 설정할 수 있습니다:

<Canvas>
  <Story name="Default">
    <InputField
      value="Default Value"
      label="Default Label"
      placeholder="Enter text..."
    />
  </Story>
</Canvas>

## 커스텀 플레이스홀더

<Canvas>
  <Story name="Custom Placeholder">
    <InputField
      value=""
      label="Custom Label"
      placeholder="Custom Placeholder..."
    />
  </Story>
</Canvas>
